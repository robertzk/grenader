// Generated by CoffeeScript 1.6.1
(function() {
  var JungleWar;

  JungleWar = (function() {

    function JungleWar() {
      var _this = this;
      this.canvas = document.getElementById('canvas');
      this.context = this.canvas.getContext('2d');
      this.drawTransparentSquare(this.context);
      this.drawSquare(this.context);
      this.canvas.addEventListener('mousemove', function(e) {
        var mousePos, msg;
        mousePos = _this.getMousePos(e);
        msg = "Mouse position: " + mousePos.x + ", " + mousePos.y;
        return _this.writeMessage(msg);
      });
    }

    JungleWar.prototype.getMousePos = function(e) {
      var rect;
      rect = this.canvas.getBoundingClientRect();
      return {
        x: e.clientX - rect.left,
        y: e.clientY - rect.top
      };
    };

    JungleWar.prototype.drawTransparentSquare = function() {
      this.context.fillStyle = 'red';
      this.context.globalAlpha = 0.5;
      return this.context.fillRect(100, 20, 300, 300);
    };

    JungleWar.prototype.drawSquare = function() {
      this.context.fillStyle = 'red';
      return this.context.fillRect(300, 50, 120, 520);
    };

    JungleWar.prototype.writeMessage = function(msg) {
      this.context.clearRect(0, 0, 500, 40);
      this.context.font = '18pt Calibri';
      this.context.fillStyle = 'black';
      return this.context.fillText(msg, 10, 25);
    };

    return JungleWar;

  })();

  $(function() {
    return new JungleWar();
  });

}).call(this);
